<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.occshottest">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.OCCSHOTtest">
        <activity android:name=".package_pricing"></activity>
        <activity android:name=".noInternet" />
        <activity android:name=".delivery" />
        <activity android:name=".status" />
        <activity android:name=".order" />
        <activity android:name=".maplocation" />
        <activity android:name=".packages_grid">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".packages"></activity>
        <activity android:name=".MainActivity" />
    </application>

</manifest>